# ASR Hub 依賴套件

# 核心依賴
yaml2py
pretty-loguru
redis-toolkit
pystorex
pydantic
python-dotenv

# 響應式和資料結構
reactivex  # PyStoreX 依賴
immutables  # PyStoreX 使用的不可變資料結構

# 系統工具
watchdog  # 文件監控（ConfigManager 使用）
redis  # Redis 客戶端
requests  # HTTP 請求
tqdm  # 進度條
schedule  # 定時任務排程
# 非同步框架
fastapi
uvicorn[standard]
aiofiles
httpx
sse-starlette
python-multipart

# WebSocket 和 Socket.io
websockets
python-socketio
aiohttp

# 音訊處理
numpy
scipy>=1.11.0  # 高品質音訊重新取樣
soundfile
librosa
pydub
pyaudio
sounddevice>=0.4.6  # 主要音訊後端
gradio>=4.0.0  # Web UI 視覺化

# GPU 加速 (選用 - 需要 CUDA)
# cupy-cuda12x  # 取消註解以啟用 GPU 支援 (需要 CUDA 12.x)
# cupy-cuda11x  # 或使用這個 for CUDA 11.x

# 視覺化和系統監控
matplotlib
psutil

# VAD 和機器學習
onnxruntime

# ASR Providers
# Whisper
openai-whisper
faster-whisper

# 測試工具（選用）
pytest
pytest-asyncio
pytest-cov
coverage
sseclient-py>=1.8.0  # SSE 客戶端測試用

# 視覺化和分析（選用）
colorama
matplotlib


# 其他工具
uuid6  # UUID v7 支援（更好的時間排序）
transitions  # 狀態機管理